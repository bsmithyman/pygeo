
def make_ext (modname, pyxfilename):
  from distutils.extension import Extension
  import numpy
  return Extension(name = modname, sources = [pyxfilename, 'filter.c'], include_dirs = [numpy.get_include()], depends=[], extra_compile_args=['-fopenmp'], extra_link_args=['-fopenmp'])
